// Root-level schema re-export for InstantDB CLI.
// The CLI expects `instant.schema.ts` at the project root.
// The actual schema definition lives in `src/lib/schema.ts`.

export { default } from './src/lib/schema'
